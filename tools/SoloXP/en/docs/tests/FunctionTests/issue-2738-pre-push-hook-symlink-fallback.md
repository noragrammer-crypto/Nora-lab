# issue-2738-pre-push-hook-symlink-fallback functional test

## Test target

`.claude/hooks/pre-push.sh` (actual synchronization behavior at runtime)

Create the original skills (`SoloXP/skills/xp_Sample`, `workflow/skills/sample-non-xp`) on the temporary git repository.
Create a fixture and actually run the hook

1. Even in **non-Termux but environment where you can't actually create symlinks** (injecting fake `ln` in PATH which always fails),
   `.claude/skills/` Subordinate skills do not disappear and fall back as physical copies (confirm correction of point 1)
2. **If there are already staged changes (unrelated WIP changes) to be synchronized**, the pre-push hook will automatically commit
   Skip, and both synchronized contents and unrelated changes remain staged (confirm correction of point 2)

(Issue #2738, Implementation Task #2936).

## Test file

`SoloXP/tests/functional/issue-2738-pre-push-hook-symlink-fallback.functional.test.js`

## Test case list

### Symlink-disabled environment (`describe('issue-2738: 非Termuxだがsymlink不可な環境では実体コピーへフォールバックする')`)

| Test case | Type | Content |
|---|---|---|
| Regression confirmation: .claude/skills/xp_Sample does not disappear even in an environment where ln -s fails | Normal system (confirmation of correction of issue 1) | fake `ln` (always fails) Skill directory exists even under injection |
| Regression confirmation: .claude/skills/xp_Sample remains as a real directory (not a symlink) | Normal system | `fs.lstatSync().isSymbolicLink()` is `false` and the contents match the original |
| Regression confirmation: The dotfiles/.claude/skills/ side is also copied correctly | Normal system (non-destructive confirmation of existing specifications) | The dotfiles side is originally always a physical copy and is not affected by this modification |
| Synchronized contents are committed and the working tree is clean | Normal system | Synchronous commit itself is successful even in an environment where symlinks are not possible |

### staged change protection (`describe('issue-2738: 同期対象外に既にstaged変更がある場合は自動コミットをスキップする')`)

| Test case | Type | Content |
|---|---|---|
| Automatic commit does not occur if there are unrelated staged changes | Normal system (confirm fix for issue 2) | HEAD does not change before and after hook execution |
| Unrelated changes (unrelated-wip.txt) remain staged | Normal system | Check `A unrelated-wip.txt` with `git status --porcelain` |
| Synchronization contents (xp_Sample) also remain as git added | Normal system (confirmation of no data loss) | Changes to be synchronized are not lost and remain staged |
| A message indicating that protection has been activated is output | Normal system (accountability to the user) | A warning message is included in the standard output |

## Setup

After saving the original copy (`SoloXP/skills/xp_Sample`,
`workflow/skills/sample-non-xp`), and `dotfiles/.claude/skills/` and `.claude/skills/` are empty.
Copy the entity of `.claude/hooks/pre-push.sh` in the state and make an initial commit (a new synchronization will always occur)
fixtures).

Tests in non-symlinkable environments include `ln` (always `exit 1`) generated by `makeFakeLnThatAlwaysFails()`
Run the hook with the directory injected at the beginning of `PATH`.For testing staged change protection, create `unrelated-wip.txt` and `git add` before running the hook,
Execute the hook after reproducing the state where there are staged changes that are not subject to synchronization.

## Coverage Summary

- Behavior verification of pre-push.sh symlink-disabled environment fallback: 4 items
- Behavior verification of pre-push.sh staged change protection: 4 items
- Total: 8 items
