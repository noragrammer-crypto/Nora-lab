---
issue: 1532
title: "[Enhancement] xp_doc_spec/UnitTests/FunctionTests/E2ETests が api/<EpicName>/ 固定パスのみ対応で非apiエピックのspec/testドキュメント生成が機能しない"
state: closed
labels: [enhancement]
author: noragrammer-crypto
created_at: 2026-06-20T03:20:22Z
epic: SoloXP
---

# #1532 [Enhancement] xp_doc_spec/UnitTests/FunctionTests/E2ETests が api/<EpicName>/ 固定パスのみ対応で非apiエピックのspec/testドキュメント生成が機能しない

## 本文

## 発見経緯

#1528（CodeCompass テストディレクトリ統一）の Documenter ステップで `xp_doc_spec` の対象を確認したところ、「`spec/` ディレクトリが存在しないため対象外」と誤判定した。実際には `CodeCompass/docs/spec/` が存在しており、誤判定の原因はスキル側のパス前提にあった。

## 問題

`dotfiles/.claude/skills/` 配下の以下4スキルが、ドキュメント出力先・スキャン対象を `api/<EpicName>/...` に固定している：

| スキル | 該当箇所 |
|---|---|
| `xp_doc_spec` | `api/<EpicName>/docs/spec/README.md` 等（dotfiles/.claude/skills/xp_doc_spec/SKILL.md:20,29,40,46,47） |
| `xp_doc_UnitTests` | `api/<EpicName>/tests/unit/`、`api/<EpicName>/docs/tests/UnitTests/`（同 SKILL.md:19,23,30） |
| `xp_doc_FunctionTests` | `api/<EpicName>/tests/functional/`、`api/<EpicName>/docs/tests/FunctionTests/`（同 SKILL.md:19,23,30） |
| `xp_doc_E2ETests` | `api/<EpicName>/tests/e2e/`、`api/<EpicName>/docs/tests/E2ETests/`（同 SKILL.md:19,23,30） |

しかし HolyAutomater は `api/` 配下が Vercel Function のエントリポイントのみを置くモノレポ構成（CLAUDE.md 参照）であり、ソース・テスト・ドキュメントは各エピックのルート直下（例: `CodeCompass/docs/spec/`、`CodeCompass/__tests__/`）に置かれる。`api/<EpicName>/` が存在しないエピック（CodeCompass・SocialMediaAgent・workflow 等の大半）では、これら4スキルが対象ディレクトリを誤検出し、ドキュメントが存在するにも関わらず「存在しない」と誤判定したり、本来更新すべきドキュメントを生成しないまま完了してしまう。

## 参考: 既に対応済みのスキル

`xp_doc_reference` は同じ問題に対して優先順位付きフォールバックで対応している（dotfiles/.claude/skills/xp_doc_reference/SKILL.md:20-26）：

```
1（優先）: api/<Epic>/ が存在する場合 → api/<Epic>/docs/reference/
2: <Epic>/scripts/ が存在する場合 → <Epic>/docs/reference/
3（フォールバック）: <Epic>/ 直下の *.js/*.py → <Epic>/docs/reference/
```

## 修正方針

`xp_doc_spec` / `xp_doc_UnitTests` / `xp_doc_FunctionTests` / `xp_doc_E2ETests` の4スキルに、`xp_doc_reference` と同様の優先順位付きパス解決ロジックを追加する：

- `api/<Epic>/` が存在する場合はそちらを優先（後方互換）
- 存在しない場合は `<Epic>/docs/spec/` `<Epic>/__tests__/` 等、エピックルート直下を対象とする

## 影響範囲

- `dotfiles/.claude/skills/xp_doc_spec/SKILL.md`
- `dotfiles/.claude/skills/xp_doc_UnitTests/SKILL.md`
- `dotfiles/.claude/skills/xp_doc_FunctionTests/SKILL.md`
- `dotfiles/.claude/skills/xp_doc_E2ETests/SKILL.md`

## 関連イシュー

#1528（発見元タスク）
#1531（このスキル不備が直接の実害につながった具体的バグ）

## コメント

### noragrammer-crypto — 2026-07-28T22:51:21Z

## 既存実装との競合チェック（手動トレース）

対象: #1532

フォールバック発動: あり
発動理由: 対象がアプリケーションコードではなく Claude Skill 定義ファイル（`dotfiles/.claude/skills/*/SKILL.md`）であり、`code-architect` サブエージェントが通常想定する codebase 構造探索の対象外と判断したため、手動トレースを直接実施した。

**Patterns & Conventions Found:**
- `xp_doc_reference`（`dotfiles/.claude/skills/xp_doc_reference/SKILL.md:20-26`）が既に優先順位付きフォールバックを実装済み：
  1. `api/<Epic>/` が存在する場合はそちら優先（後方互換）
  2. `<Epic>/scripts/` が存在する場合
  3. `<Epic>/` 直下の `*.js`/`*.py`（フォールバック）
- `xp_doc_E2ETests`（`dotfiles/.claude/skills/xp_doc_E2ETests/SKILL.md:19-22`）は本イシュー報告時点から既に別修正で `api/<Epic>/` 固定パスを脱しており、`<Epic>/tests/e2e/` または `<Epic>/__tests__/e2e/` の実在する方を使う2択フォールバックに変わっている（未着手なのは `xp_doc_spec` / `xp_doc_UnitTests` / `xp_doc_FunctionTests` の3ファイルのみ）。
- 実リポジトリの実態調査（CodeCompass / SocialMediaAgent / workflow / DiscordAIbot / DiscordBotDashboard）: **`api/<Epic>/tests/...` や `api/<Epic>/docs/spec/` は現存するどのエピックにも存在しない。** 全エピックが `<Epic>/__tests__/{unit,functional,e2e}/` と `<Epic>/docs/spec/` / `<Epic>/docs/tests/{UnitTests,FunctionTests,E2ETests}/` を使っている（`api/` 配下は Vercel Function エントリポイント専用というCLAUDE.mdの構成規約通り）。

**Architecture Decision:**
`xp_doc_spec` / `xp_doc_UnitTests` / `xp_doc_FunctionTests` の3スキルに、`xp_doc_reference` と同型の優先順位付きフォールバックを追加する：
1. `api/<Epic>/` が存在する場合はそちら優先（後方互換）
2. 存在しない場合は `<Epic>/` 直下（`__tests__/unit/` 等）をスキャン対象・出力先とする

`xp_doc_E2ETests` は対応済みのためスコープ外（変更不要）。

競合の有無: なし（既存パターンの延長線上の変更）

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:51:31Z

## E2E/spec 要否判定（Task）

対象: #1532
イシュー種別: Task（単一の実装パターンを3ファイルに機械的に適用する単発修正のため、Storyへの分解は不要と判断）
観測可能な振る舞いの変更: あり（`xp_doc_spec`/`xp_doc_UnitTests`/`xp_doc_FunctionTests` のスキャン対象・ドキュメント出力先が変わり、非apiエピックでもドキュメント生成が機能するようになる）
追加タスク: なし

理由: 変更対象は Claude Skill の指示書（`SKILL.md`）自体であり、アプリケーションコードのような自動テスト（jest等）インフラが存在しない領域。かつ `xp_doc_reference` に既に同型の実装パターンが存在し、`xp_doc_E2ETests` も別修正で同種のフォールバックへ移行済み（本イシューのコメント参照）。新規の設計判断を要さない機械的な横展開のため、独立した E2Eテストスイート作成タスク・機能仕様書更新タスクは発行せず、本イシュー（#1532）を単一のTaskとしてそのまま直接実装する。受け入れ判定は `xp_Auditor doc`（3ファイルの記述が意図通りフォールバックロジックを反映しているかのレビュー）で行う。

親ブランチ: なし（ルートタスクとして `--base main` でPRを発行する）

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:51:42Z

作業開始 2026-07-29 07:51 JST
[ProjectStatus: InProgress]

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:51:53Z

[Tester実行中]

## テスト実行結果

対象: `dotfiles/.claude/skills/xp_doc_spec/SKILL.md`, `dotfiles/.claude/skills/xp_doc_UnitTests/SKILL.md`, `dotfiles/.claude/skills/xp_doc_FunctionTests/SKILL.md`（および `SoloXP/skills/` 配下の同名ミラー）

判定: ドキュメントのみの変更（Claude Skill 指示書=Markdown。実行可能なアプリケーションコードを含まない）
処理: 全層スキップ（テスト対象コードなし）

- Unit Test: スキップ
- Functional Test: スキップ
- E2E Test: スキップ（Taskイシューであり親ストーリーなし）

[Tester完了]

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:52:00Z

[Implementer実行中]

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:53:06Z

## 実装完了

`xp_doc_reference` / 修正済み `xp_doc_E2ETests` と同型の優先順位付きフォールバック（1. `api/<Epic>/` 優先＝後方互換 → 2. `<Epic>/` 直下）を、以下3スキル × 3コピー（計9ファイル）に追加した：

変更ファイル:
- `SoloXP/skills/xp_doc_spec/SKILL.md`
- `SoloXP/skills/xp_doc_UnitTests/SKILL.md`
- `SoloXP/skills/xp_doc_FunctionTests/SKILL.md`
- `.claude/skills/xp_doc_spec/SKILL.md`
- `.claude/skills/xp_doc_UnitTests/SKILL.md`
- `.claude/skills/xp_doc_FunctionTests/SKILL.md`
- `dotfiles/.claude/skills/xp_doc_spec/SKILL.md`
- `dotfiles/.claude/skills/xp_doc_UnitTests/SKILL.md`
- `dotfiles/.claude/skills/xp_doc_FunctionTests/SKILL.md`

変更内容（3スキル共通）:
- 「対象ディレクトリを決定する」ステップを先頭に追加し、`api/<Epic>/...` が存在すればそちらを優先（後方互換）、存在しなければ `<Epic>/...`（実在するエピックが実際に使う配置：`__tests__/unit/`, `__tests__/functional/`, `docs/spec/`）をスキャン対象・出力先とするよう変更
- 以降の手順本文は決定済みディレクトリを指す表現に統一

テスト結果:
- 単体テスト: 対象外（Claude Skill 指示書=Markdownのみの変更、実行可能コードなし。Testerコメント参照）
- 機能テスト: 対象外（同上）

[Implementer完了]

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:53:31Z

[Auditor テスト実行中]

対象: `xp_doc_spec` / `xp_doc_UnitTests` / `xp_doc_FunctionTests` の各 `SKILL.md`（Claude Skill 指示書。実行可能なアプリケーションコードなし）
テスト実行: 対象外（xp_RunTestSuites の対象となるコード変更が存在しないため。Testerの判定「ドキュメントのみの変更 → 全層スキップ」を追認）

差分レビュー結果:
- `api/<Epic>/` 優先 → `<Epic>/` 直下フォールバックの2段階ロジックが3スキル共通で正しく追加されている
- フォールバック先パス（`<Epic>/__tests__/unit/`, `<Epic>/__tests__/functional/`, `<Epic>/docs/spec/`）は既存の `xp_doc_reference` の設計思想（Architectコメント参照）および実リポジトリの実態調査結果と一致している
- `SoloXP/skills/` / `.claude/skills/` / `dotfiles/.claude/skills/` の3コピーがバイト単位で一致していることを確認済み
- 既存の `api/<Epic>/` 優先パスは維持されており後方互換性あり

問題なし。

[Auditor GREEN]
根拠: ドキュメントのみの変更のため自動テストは対象外。差分レビューにより意図通りの実装であることを確認した。

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:53:40Z

作業完了 2026-07-29 07:53 JST / 所要時間: 2分

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:55:31Z

[Documenter実行中]

## ドキュメント生成完了

生成・更新したファイル:
- `SoloXP/docs/issues/issue-1532.MD`（新規作成 — xp_issue2md）
- `SoloXP/docs/spec/xp_doc_generation.md`（新規作成 — xp_doc_spec: `xp_doc_*` 系ドキュメント生成スキル群のパス解決優先順位を仕様として記録）
- `SoloXP/docs/spec/README.md`（更新 — 索引に `xp_doc_generation.md` を追加）

変更なし（対象コードなしのため）:
- `xp_doc_reference` / `xp_doc_UnitTests` / `xp_doc_FunctionTests` / `xp_doc_E2ETests`（epic: SoloXP）— 本タスクで変更したのは `.claude/skills/`・`dotfiles/.claude/skills/`・`SoloXP/skills/` 配下の SKILL.md のみで、`SoloXP/lib`・`SoloXP/__tests__` 等のアプリケーションコードは変更していないため、これらの走査対象に差分なし

[Documenter完了]

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:55:56Z

[Auditor ドキュメントチェック中]

### ドキュメントチェック結果
- spec/README.md: OK（索引に `xp_doc_generation.md` の行を追加済み、リンク先ファイル名一致）
- spec/xp_doc_generation.md: OK（新規作成。api/優先→フォールバックの解決順位・対象スキルごとの具体パスを記録。空疎な内容ではない）
- reference/: N/A（本タスクはSKILL.mdのみの変更でSoloXPアプリケーションコードに変更なし。対象コードなし）
- issue2mdログ: OK（`SoloXP/docs/issues/issue-1532.MD` 存在。Documenter実行時点のコメント8件を記録済み。本チェック時点でのGitHub側コメント数は9件だが、差分はDocumenter自身の完了報告コメント1件分であり、ログ生成後に追加された自己言及コメントのため陳腐化とは判定しない）

[Auditor doc OK]
xp_Director がPRを発行します。

---
_Generated by [Claude Code](https://claude.ai/code)_

---

### noragrammer-crypto — 2026-07-28T22:56:47Z

[PR発行済み #2456]
[ProjectStatus: Done]

PR: https://github.com/noragrammer-crypto/HolyAutomater/pull/2456（`--base main`, `Closes #1532`）
マージ後にGitHubが自動クローズします。オーナーによるマージをお待ちください。

---
_Generated by [Claude Code](https://claude.ai/code)_

---
