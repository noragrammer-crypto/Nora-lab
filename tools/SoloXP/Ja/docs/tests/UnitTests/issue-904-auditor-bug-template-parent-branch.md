# issue-904-auditor-bug-template-parent-branch ユニットテスト

## テスト対象

`~/.claude/skills/xp_Auditor/SKILL.md`

xp_Auditor が自動起票するバグイシューのテンプレートに、親ブランチ・親ストーリーフィールドが
含まれていることを検証する（Issue #904 のバグ再現・回帰防止）。

## テストファイル

`SoloXP/__tests__/issue-904-auditor-bug-template-parent-branch.unit.test.js`

## テストケース一覧

### `ステップ5「別タスクスコープのバグ発見時」セクションの gh issue create テンプレート`

| テストケース | 種別 | 内容 |
|---|---|---|
| 「別タスクスコープのバグ発見時」セクションが存在する | 正常系 | SKILL.md に `別タスクスコープのバグ発見時` セクションが存在すること |
| gh issue create テンプレートに ## 親ストーリー セクションが含まれる | 正常系 | バグ起票テンプレートに `## 親ストーリー` フィールドがあること |
| gh issue create テンプレートに ## 親ブランチ セクションが含まれる | 正常系 | バグ起票テンプレートに `## 親ブランチ` フィールドがあること |

### `ステップ3「Story-level Auditor フェーズ」RED ケース`

| テストケース | 種別 | 内容 |
|---|---|---|
| Story-level Auditor フェーズのセクションが存在する | 正常系 | `E2E テストの判断・Story-level Auditor フェーズ` セクションが存在すること |
| RED ケースでバグイシュー起票時に ## 親ブランチ の記述がある | 正常系 | E2E 失敗時のバグ起票に `## 親ブランチ` フィールドがあること |

## 実装メモ

- セクション抽出の正規表現はセクション番号に依存しない形で記述する（`\d+\.` を使用）
  - SKILL.md のセクション追加による番号変更に耐性を持たせるため（Issue #1279 で修正）
- `Story-level Auditor フェーズ` のセクション（### 3.）は番号を固定して抽出（現時点で変更なし）

## カバレッジサマリー

- xp_Auditor バグ起票テンプレートの必須フィールド検証: 3件
- Story-level Auditor フェーズの RED ケース検証: 2件
- 合計: 5件
